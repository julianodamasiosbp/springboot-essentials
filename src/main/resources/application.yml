spring:
  application:
    name: springboot-essentials
  datasource:
    url: jdbc:mysql://localhost:3306/anime?allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  error:
    include-stacktrace: always

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
